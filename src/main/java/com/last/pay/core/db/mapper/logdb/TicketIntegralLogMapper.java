package com.last.pay.core.db.mapper.logdb;

import com.last.pay.core.db.pojo.log.TicketIntegralLog;

public interface TicketIntegralLogMapper {
	
	void insertTicketIntegeralLog(TicketIntegralLog ticketIntegralLog);

}
